# Frontend environment — copy to .env.local. Baked into the build at build time.
# Local dev points at the local NestJS API; production is https://rezwoan.me (set on the Pi).
NEXT_PUBLIC_API_URL=http://localhost:3201
NEXT_PUBLIC_SITE_URL=http://localhost:3200
